# Remove Real Entra ID Values from appsettings

## Description
Purge all real Microsoft Entra ID (Azure AD) values from `appsettings.json`. Add to `.gitignore` and update README to clarify local dev setup.

## Acceptance Criteria
- No real Entra ID values in source
- `.gitignore` covers local secrets
- README documents safe config
